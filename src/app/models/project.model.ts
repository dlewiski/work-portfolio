export class Project {
  constructor(public projectTitle: string, public description: string, public challenges: string, public gitURL: string, public deployedURL: string, public photoHeader: string, public photoArray=[]) { }
}
